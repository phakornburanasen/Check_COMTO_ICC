# CHECKCOM Backend

Go API for the CHECKCOM frontend.

## Run

```bat
go run .
```

The API listens on `0.0.0.0:10300`.

## Build

```bat
build.bat
```

Cross-compiles both targets into `dist/`:
- `dist/CHECKCOM_API.exe` (windows/amd64)
- `dist/CHECKCOM_API` (linux/amd64)
